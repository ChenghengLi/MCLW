% 07_experiments.tex

\section{Experiments}
\label{sec:experiments}

We empirically validate the three principal predictions of
\autoref{thm:detection}, \autoref{thm:robustness}, and
\autoref{thm:k-regular} on a fleet of generations spanning three
instruct-tuned 7--8B models, four content domains, and four attack
conditions. The campaign comprises four complementary
sub-experiments. Exp.~2 (\autoref{subsec:exp2-headline}) is a
head-to-head comparison against the canonical KGW baseline and the
SWEET high-entropy gate. Exp.~3 (\autoref{subsec:exp3-translation})
measures robustness under machine-translation pivots. Exp.~4
(\autoref{subsec:exp4-kregular}) sanity-checks the $k$-regular
generalisation at $k{=}2$. Exp.~5
(\autoref{subsec:exp5-calibration}) anchors the closed-form FPR
calibration via a sweep over the state count $S$. All computation
runs on the EPFL RunAI cluster; per-prompt records (logits, gate
decisions, post-attack tokens, $z$-scores) stream to JSONL for
reproducibility.

\subsection{Setup}
\label{subsec:exp-setup}

\textbf{Models.} We use three instruction-tuned, openly licensed
checkpoints in the 7--8B parameter range:
\texttt{meta-llama/Llama-3.1-8B-Instruct},
\texttt{Qwen/Qwen-2.5-7B-Instruct}, and
\texttt{mistralai/Mistral-7B-Instruct-v0.3}. The same secret key
$\kappa$ is fixed across every run.

\textbf{Domains.} Four prompt domains stress different entropy
regimes: \emph{code} (HumanEval problem stems), \emph{factual}
(short closed-form knowledge prompts), \emph{wiki} (open-ended
``Explain $X$\ldots'' prompts over a curated concept list), and
\emph{writing} (creative-completion prompts). Headline experiments
use 100 prompts per domain; calibration and translation experiments
use a 25-prompt subsample per domain to keep the design balanced
under the larger Cartesian product.

\textbf{Generation.} Temperature $T = 0.7$, top-$p = 1$, token
budget $n = 200$ at every cell. Prompt order is determined by a
deterministic seed so the same indices are watermarked, attacked,
and detected across methods.

\textbf{MCL configuration.} Unless stated otherwise, MCL runs use
$S = 5$ states, target gate budget $\rho = 0.5$, the
high-uncertainty gate $G_{H_{\mathrm{high}}}$, and clockwork
($k{=}1$) topology. \autoref{subsec:exp4-kregular} varies $k$ and
\autoref{subsec:exp5-calibration} varies $S$.

\textbf{Detection.} The detection statistic and the threshold
$z_\alpha$ are taken directly from \autoref{thm:detection};
the same threshold is reused across methods for fair comparison.

\subsection{Headline Comparison: MCL vs KGW vs SWEET}
\label{subsec:exp2-headline}

The headline experiment (Exp.~2) is a $3 \times 3 \times 4 \times 4$
factorial: 3 watermarking methods $\times$ 3 models $\times$ 4
domains $\times$ 4 attack conditions, with 100 prompts per
(method, model, domain) cell.

\textbf{Methods.} \emph{MCL} is configured at the canonical
operating point $S{=}5$, $\rho = 0.5$, gate $G_{H_{\mathrm{high}}}$,
clockwork ($k{=}1$). \emph{KGW}~\citep{kirchenbauer2023watermark}
uses its standard parameters $\gamma = 0.5$ and logit bias
$\delta_{\mathrm{KGW}} = 2$. \emph{SWEET}~\citep{lee2023sweet} retains the same
$\gamma = 0.5$, $\delta_{\mathrm{KGW}} = 2$ but applies the watermark only at
positions whose token-level entropy exceeds a threshold calibrated
to gate $\rho = 0.5$ of positions; this is the high-entropy gate
that aligns with our $G_{H_{\mathrm{high}}}$ but in the
green/red-list scheme.

\textbf{Attacks.} Each generated text is scored under three
conditions: \emph{clean} (no attack); \emph{random-substitution} at
effective edit distance $\delta_{\mathrm{eff}} = 0.20$; and
\emph{back-translation} via NLLB-200 EN$\to$ZH$\to$EN. For the
back-translation, the empirical $\delta_{\mathrm{eff}}$ is recorded
per output via token-edit distance to the original.

\textbf{Reporting.} The primary metric is true-positive rate at a
calibrated $\mathrm{FPR} = 0.01$ (TPR@$1\%$), computed against a
matched non-watermarked baseline drawn from the same model,
domain, and prompt set.

\begin{figure*}[t]
  \centering
  \includegraphics[width=0.95\linewidth]{pictures/fig_headline_tpr.pdf}
  \caption{\textbf{Head-to-head TPR comparison across attacks.}
    MCL ($S{=}5$, $\rho{=}0.5$, $G_{H_{\mathrm{high}}}$, $k{=}1$)
    vs KGW ($\gamma{=}0.5, \delta_{\mathrm{KGW}}{=}2$) vs SWEET (same KGW params,
    high-entropy gating at $\rho{=}0.5$). Aggregated over
    Llama-3.1-8B, Qwen-2.5-7B, Mistral-7B-v0.3 and four domains
    (code / factual / wiki / writing).}
  \label{fig:headline-tpr}
\end{figure*}

\begin{table}[t]
  \centering
  \caption{\textbf{Headline aggregate.} Detection rate at the analytic
    threshold $z_\alpha = 2.326$ ($\alpha = 0.01$ under the Gaussian-tail
    null), aggregated across three instruction-tuned 7--8B LLMs
    (Llama-3.1-8B, Qwen-2.5-7B, Mistral-7B) and four text domains
    (HumanEval code, factual, wiki, writing) with 100 prompts per
    (method, model, domain) cell at $n=200$ tokens. \textbf{MCL} (ours):
    $S=5$, $\rho=0.5$, $G_{H_{\mathrm{high}}}$. \textbf{KGW}:
    $\gamma=0.5$, $\delta_{\mathrm{KGW}}=2$. \textbf{SWEET}: KGW with
    high-entropy gate at $\rho=0.5$ matched to MCL. Median PPL is on
    the watermarked model's own logits (see \autoref{sec:discussion}
    for caveats). The SWEET aggregate is over $9$ complete cells
    (Qwen-2.5-7B factual/wiki/writing in flight at submission); MCL/KGW
    cover all $12$. Empirical FPR drift at this threshold is reported
    separately in \autoref{tab:cal-sstar}. Bold = best per column.}
  \label{tab:headline-aggregate}
  \small
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{lccccc}
    \toprule
    Method & cells & TPR$_{\mathrm{clean}}$ & TPR$_{\mathrm{random}}$ &
            TPR$_{\mathrm{ZH}}$ & PPL ($\downarrow$) \\
    \midrule
    \textbf{MCL (ours)} & 12/12 & \textbf{100.0\%} & \textbf{99.9\%} & \textbf{72.8\%} & 3.10 \\
    KGW                 & 12/12 & 76.5\%  & 56.2\%  & 21.9\%  & 1.85 \\
    SWEET               &  9/12 & 65.6\%  & 45.2\%  & 18.8\%  & \textbf{1.68} \\
    \bottomrule
  \end{tabular}
\end{table}

\autoref{fig:headline-tpr} and \autoref{tab:headline-aggregate}
collapse the design over the (model, domain) axis; the per-cell
breakdown is in \autoref{tab:headline-percell}. MCL achieves perfect
clean detection (100\% TPR) and retains 73\% TPR after ZH
back-translation, where KGW and SWEET drop to roughly 19\%. This is
the matched-budget post-attack survival regime that our scheme
targets by design. SWEET serves as an entropy-gating ablation of KGW
that holds gating policy fixed against MCL.

\begin{table*}[t]
  \centering
  \caption{\textbf{Per-(model, domain) head-to-head TPR (\%) at the
    analytic $z_\alpha=2.326$ threshold.} 100 prompts per cell, $n=200$
    tokens. MCL leads in every clean cell (perfect detection) and in
    every cell post-ZH-back-translation. Bold = best per column within
    each block.}
  \label{tab:headline-percell}
  \small
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{ll|rrr|rrr}
    \toprule
    & & \multicolumn{3}{c|}{Clean} & \multicolumn{3}{c}{ZH back-translation} \\
    Model & Domain & MCL & KGW & SWEET & MCL & KGW & SWEET \\
    \midrule
    Llama-3.1-8B & code     & \textbf{100} & 57 & 41 & \textbf{56} & 24 & 18 \\
    Llama-3.1-8B & factual  & \textbf{100} & 93 & 80 & \textbf{75} & 30 & 22 \\
    Llama-3.1-8B & wiki     & \textbf{100} & 91 & 69 & \textbf{76} & 19 & 11 \\
    Llama-3.1-8B & writing  & \textbf{100} & \textbf{100} & \textbf{100} & \textbf{76} & 33 & 20 \\
    Qwen-2.5-7B  & code     & \textbf{100} & 40 & 34 & \textbf{58} & 26 & 22 \\
    Qwen-2.5-7B  & factual  & \textbf{100} & 100 & --- & ---  & ---  & ---  \\
    Qwen-2.5-7B  & wiki     & \textbf{100} & 100 & --- & ---  & ---  & ---  \\
    Qwen-2.5-7B  & writing  & \textbf{100} & 100 & --- & ---  & ---  & ---  \\
    Mistral-7B   & code     & \textbf{100} & 54 & 44 & \textbf{78} & 15 & 10 \\
    Mistral-7B   & factual  & \textbf{100} & 89 & 80 & \textbf{71} & 31 & 29 \\
    Mistral-7B   & wiki     & \textbf{100} & 52 & 44 & \textbf{70} & 18 &  9 \\
    Mistral-7B   & writing  & \textbf{100} & 96 & 97 & \textbf{73} & 25 & 29 \\
    \bottomrule
  \end{tabular}
\end{table*}

\subsection{Robustness Decay under Translation}
\label{subsec:exp3-translation}

Exp.~3 tests the closed-form $(1-\delta)^2$ scaling of
\autoref{thm:robustness} on real machine-translation attacks.
Each MCL-watermarked output (3 models $\times$ 4 domains $\times$
25 prompts $\times$ $n{=}200$ tokens) is round-tripped through
NLLB-200~\citep{nllbteam2022nllb} on four pivot languages: French,
German, Russian, and Chinese. The empirical effective edit
distance $\delta_{\mathrm{eff}}$ is computed from the token-edit
distance between the watermarked output and its post-pivot
counterpart, giving each generation a continuous $\delta$ value
rather than a single nominal rate.

\autoref{thm:robustness} predicts that the post-attack $z$-score
satisfies $z_{\mathrm{post}}/z_{\mathrm{clean}} = (1 -
\delta_{\mathrm{eff}})^2$ in expectation, modulo finite-sample
Bernoulli noise. We plot the observed ratio against the
predicted $(1-\delta_{\mathrm{eff}})^2$ on a single set of axes,
overlaying the slope-1 identity.

\begin{figure*}[t]
  \centering
  \includegraphics[width=\linewidth]{pictures/fig_translation_decay.pdf}
  \caption{\textbf{Translation robustness curve.} Empirical
    detection-decay ratio under NLLB-200 EN$\to$\{FR, DE, RU,
    ZH\}$\to$EN pivots, with $\delta_{\mathrm{eff}}$ measured per
    generation. The closed-form $(1-\delta_{\mathrm{eff}})^2$
    prediction from \autoref{thm:robustness} is the diagonal.
    Per-pivot TPR breakdown in \autoref{app:translation-pivots}.}
  \label{fig:translation-decay}
\end{figure*}

If the slope-1 prediction holds, this anchors the universal
half-life $\delta^\star = 1 - 1/\sqrt{2}$ empirically: detection
power halves at $\delta_{\mathrm{eff}} \approx 0.293$ regardless of
model, domain, or pivot language. The claim is stronger than
synthetic random-substitution attacks alone can support, since
NLLB-200 introduces semantically coherent rewrites rather than
i.i.d.\ token noise.

\subsection{$k$-Regular Generalisation}
\label{subsec:exp4-kregular}

Exp.~4 verifies the $k$-regular extension of MCL predicted by
\autoref{thm:k-regular}: at any $k$-regular soft-cycle topology,
the random-baseline fingerprint mass is $p_0 = k/S$, the detection
$z$-score scales as in \autoref{thm:detection} with this updated
null mean, and the universal post-attack $\delta^\star$ is invariant
in $k$. We instantiate $k = 2$ (each state has exactly two allowed
successors) at $S = 5$, predicting $p_0 = 2/5 = 0.40$, on the wiki
domain (100 prompts) for each of the three models, with a ZH
back-translation as the only attack condition (the most frequent
attack regime on the open web).

\begin{table}[t]
  \centering
  \caption{\textbf{$k$-regular sanity check at $k{=}2$ (soft-cycle), $S{=}5$,
    $\rho{=}0.5$.} Predicted random baseline $p_0 = k/S = 0.40$. Wiki
    domain, 100 prompts. Confirms the $k$-regular extension of
    \autoref{thm:detection} (formal: \autoref{thm:k-regular},
    \autoref{app:proofs}).}
  \label{tab:kregular}
  \small
  \setlength{\tabcolsep}{3.5pt}
  \begin{tabular}{lrrrrr}
    \toprule
    Model & $\bar\phi_{\mathrm{cl}}$ & $\bar z_{\mathrm{cl}}$ & TPR$_{\mathrm{cl}}$
          & $\bar z_{\mathrm{ZH}}$ & TPR$_{\mathrm{ZH}}$ \\
    \midrule
    Llama-3.1-8B & 0.883 & 13.91 & 100\% & 3.19 & 54\% \\
    Mistral-7B   & 0.965 & 16.23 & 100\% & 3.91 & 70\% \\
    Qwen-2.5-7B  & 0.934 & 15.38 & 100\% & 4.12 & 71\% \\
    \bottomrule
  \end{tabular}
\end{table}

A close match between observed $p_0$ on unwatermarked text and the
$k/S$ prediction certifies the random-oracle assumption underlying
\autoref{thm:k-regular}; a clean-vs-ZH $z$-score ratio close to
$(1 - \delta_{\mathrm{eff}})^2$ on the same generations
re-certifies \autoref{thm:robustness} at a non-trivial $k$.

\subsection{Empirical Calibration Anchor (S sweep)}
\label{subsec:exp5-calibration}

Exp.~5 anchors the closed-form FPR calibration of
\autoref{thm:detection} empirically. We sweep $S \in \{2, 3, 5,
7, 11\}$ across the three models, with 4 domains $\times$ 25
prompts $\times$ $n{=}200$ tokens per cell. Each cell records both
a watermarked generation (MCL at the canonical operating point,
varying only $S$) and a non-watermarked baseline drawn from the
same prompt with the same model. The non-watermarked outputs feed
the empirical FPR estimate at the analytic threshold $z_\alpha$;
the watermarked outputs feed the TPR.

\autoref{thm:detection} predicts that the null-distribution
$z$-score satisfies a Hoeffding-style upper bound of
$\mathrm{FPR} \leq \exp(-2 z_\alpha^2)$ once $S$ is large enough
that the gated-position fingerprint mean is well-separated from
$1/S$; the minimum admissible state count $S^\star$ is the closed
form derived in \autoref{app:proofs}.

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{pictures/fig_cal_fpr.pdf}
  \caption{\textbf{Calibration anchor.} Empirical FPR on
    non-watermarked completions as a function of state count $S$,
    with the closed-form Hoeffding upper bound from
    \autoref{thm:detection}. Threshold fixed at $z_\alpha$ for
    $\alpha = 0.01$.}
  \label{fig:cal-fpr}
\end{figure}

\begin{table}[t]
  \centering
  \caption{\textbf{Empirical anchor of the calibration map
    $S^\star(n,\rho,\alpha)$ from \autoref{thm:detection}.} For each $S$,
    aggregated across 3 LLMs $\times$ 4 domains $\times$ 25 prompts
    ($\approx$ 300 watermarked and 300 non-watermarked samples; some
    cells in flight at submission). $z_{\mathrm{pred}} =
    \rho\sqrt{(S{-}1)(n{-}1)}$ from Theorem~1. Watermarked $z$
    overshoots theory by 50--90\,\% (signal exceeds the analytic lower
    bound). Empirical FPR exceeds the 1\,\% target due to non-i.i.d.\
    structure in natural-language token sequences --- the calibration
    formula gives the right ordering but a 20--30\,\% threshold
    inflation is recommended in deployment.}
  \label{tab:cal-sstar}
  \small
  \setlength{\tabcolsep}{4.5pt}
  \begin{tabular}{rrrrr}
    \toprule
    $S$ & $\bar z_{\mathrm{wm}}^{\mathrm{obs}}$
        & $z_{\mathrm{wm}}^{\mathrm{pred}}$
        & TPR$_{\mathrm{wm}}$ & FPR$_{\mathrm{nwm}}$ \\
    \midrule
    2  & 10.86 & 7.05  & 100\% & 2.18\% \\
    3  & 17.41 & 9.97  & 100\% & 2.00\% \\
    5  & 25.38 & 14.11 & 100\% & 1.67\% \\
    7  & 30.61 & 17.28 & 100\% & 6.91\% \\
    11 & 41.49 & 22.30 & 100\% & 5.33\% \\
    \bottomrule
  \end{tabular}
\end{table}

The empirical FPR curve in \autoref{fig:cal-fpr} tracks the analytic
shape but sits above the $\alpha=1\%$ target at $S\geq 7$
(\autoref{tab:cal-sstar}, $1.7$--$6.9\%$). The calibration formula
gives the right ordering and the right $S^\star$ to within one
state, but a deployer targeting strict $1\%$ FPR closes the residual
gap with the empirical-SD threshold recalibration discussed in
\autoref{sec:discussion} and \autoref{app:fpr-recipes}. The
closed-form derivation lives in \autoref{app:proofs}.

\subsection{Results Summary}
\label{subsec:results-summary}

The four sub-experiments together cover the three predictive axes
of the theory: \autoref{thm:detection} (calibration; Exp.~5),
\autoref{thm:robustness} (post-attack decay; Exp.~3), and
\autoref{thm:k-regular} (topology generalisation; Exp.~4), with
Exp.~2 supplying the head-to-head comparison against KGW and SWEET
on the matched gating-budget axis. The data in
\autoref{fig:headline-tpr},
\autoref{fig:translation-decay},
\autoref{fig:cal-fpr},
\autoref{tab:headline-aggregate},
\autoref{tab:headline-percell},
\autoref{tab:kregular}, and
\autoref{tab:cal-sstar} support four claims: (i) MCL achieves
perfect clean detection (100\% TPR) and dominates KGW/SWEET under
random-substitution and translation attacks (73\% vs 19\% TPR
post-back-translation); (ii) the post-attack $z$ ratio is consistent
with the $(1-\delta_{\mathrm{eff}})^2$ scaling across pivots, models,
and domains; (iii) the random baseline $p_0 = k/S$ holds empirically
at $k{=}2$; and (iv) the analytic FPR bound is qualitatively
actionable, with observed $S^\star$ tracking the closed form modulo
a 20--30\% empirical-threshold inflation (\autoref{sec:discussion}).
